package com.fiz.android.battleinthespace

import android.content.Context
import android.content.SharedPreferences
import androidx.appcompat.app.AppCompatActivity
import org.junit.Assert.*
import org.junit.Test

class StateTest{
//    @Test
//    fun overlap1() {
//        val state:State=State(30.0,30.0, AppCompatActivity().getSharedPreferences("data", Context.MODE_PRIVATE))
//        state.overlap1(state.spaceShips[0],state.spaceShips[1])
//        assertFalse(state.overlap1(state.spaceShips[0],state.spaceShips[1]))
//    }
//
//    @Test
//    fun overlap2() {
//        val state:State=State(30.0,30.0, AppCompatActivity().getSharedPreferences("data", Context.MODE_PRIVATE))
//        state.overlap1(state.spaceShips[0],state.spaceShips[1])
//        assertFalse(state.overlap1(state.spaceShips[0],state.spaceShips[1]))
//    }
}